# Backend API URL. Required for API calls.
#
# - Desktop (same machine):  http://127.0.0.1:8000
# - Mobile on same WiFi:    http://YOUR_PC_LAN_IP:8000   (e.g. http://192.168.1.5:8000)
# - Production (e.g. Netlify + Railway): your backend URL, e.g. https://your-api.railway.app
#
# Get your PC's LAN IP: Windows: ipconfig → "IPv4 Address"   Mac/Linux: ifconfig or ip addr
# After changing this, restart the Vite dev server (npm run dev).
VITE_API_URL=http://127.0.0.1:8000
